#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, e;
        cin >> n >> e;
        vector<vector<pair<int, int>>> g;
        while (e--)
        {
            int a, b, c;
            cin >> a >> b >> c;
        }
    }
}